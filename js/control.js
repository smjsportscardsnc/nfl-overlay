const OVERLAY_PATH="smjOverlay/current",GRAPHIC_COMMAND_PATH="smjOverlay/graphicCommand",DEFAULT_STATE={title:"SMJ NFL MIXER BREAK",status:"Live",ticker:"Welcome to SMJ Sports Cards & Collectibles!",sold:{},graphicCommand:null,league:"nfl"};let state={...DEFAULT_STATE},ref=null,graphicRef=null;const teamControls=document.getElementById("teamControls"),titleInput=document.getElementById("titleInput"),statusInput=document.getElementById("statusInput"),tickerInput=document.getElementById("tickerInput"),connectionStatus=document.getElementById("connectionStatus"),commandStatus=document.getElementById("commandStatus");function setConn(t,c){connectionStatus.textContent=t;connectionStatus.className=`connection ${c}`}function setCmd(t,c="ok"){commandStatus.textContent=t;commandStatus.className=`command-status ${c}`}function currentLeague(){return window.SMJ_LEAGUES[state.league||"nfl"]||window.SMJ_LEAGUES.nfl}function teams(){return currentLeague().teams}function logo(abbr){const lg=currentLeague();return `https://a.espncdn.com/i/teamlogos/${lg.cdn}/500/${abbr}.png`}function hydrate(){titleInput.value=state.title||DEFAULT_STATE.title;statusInput.value=state.status||DEFAULT_STATE.status;tickerInput.value=state.ticker||DEFAULT_STATE.ticker}function renderTeams(){teamControls.innerHTML="";teams().forEach(team=>{const btn=document.createElement("button");btn.type="button";btn.className=`team-btn ${state.sold?.[team.abbr]?"sold":""}`;btn.innerHTML=`<img src="${logo(team.abbr)}" alt="" /><span class="team-name"><strong>${team.name}</strong><span>${team.city}</span></span>`;btn.addEventListener("click",()=>toggleSold(team.abbr));teamControls.appendChild(btn)})}async function triggerGraphic(key){if(!ref||!graphicRef){setCmd("Firebase not connected. Command not sent.","bad");return}const label=key==="stash"?"Stash or Pass":"Spin 2 Choose 1",cmd={key,id:`${Date.now()}-${Math.random().toString(16).slice(2)}`};setCmd(`Sending ${label}...`,"warning");try{await graphicRef.set(cmd);await ref.child("graphicCommand").set(cmd);setCmd(`Sent ${label} at ${new Date().toLocaleTimeString()}`,"ok")}catch(err){console.error(err);setCmd("Firebase write failed. Check rules.","bad")}}function switchLeague(league){state.league=league;state.sold={};renderTeams();const leagueName=window.SMJ_LEAGUES[league].name,title=`SMJ ${leagueName} BREAK`;titleInput.value=title;if(ref){ref.update({league,sold:{},title});setCmd(`League switched to ${leagueName}`,"ok")}}function toggleSold(abbr){const sold={...(state.sold||{})};sold[abbr]=!sold[abbr];state.sold=sold;renderTeams();if(ref)ref.child("sold").set(sold)}function resetSold(){if(!confirm("Reset all teams to available?"))return;state.sold={};renderTeams();if(ref)ref.child("sold").set({})}function saveText(){const payload={title:titleInput.value.trim()||DEFAULT_STATE.title,status:statusInput.value.trim()||DEFAULT_STATE.status,ticker:tickerInput.value.trim()||DEFAULT_STATE.ticker};state={...state,...payload};if(ref)ref.update(payload).then(()=>setCmd("Overlay text updated.","ok"))}document.querySelectorAll("[data-graphic]").forEach(btn=>btn.addEventListener("click",()=>triggerGraphic(btn.dataset.graphic)));document.querySelectorAll("[data-league]").forEach(btn=>btn.addEventListener("click",()=>switchLeague(btn.dataset.league)));document.getElementById("saveTextBtn").addEventListener("click",saveText);document.getElementById("resetAllBtn").addEventListener("click",resetSold);hydrate();renderTeams();if(window.SMJFIREBASE_READY&&window.smjDB){ref=window.smjDB.ref(OVERLAY_PATH);graphicRef=window.smjDB.ref(GRAPHIC_COMMAND_PATH);setConn("Firebase connected","ok");setCmd("Ready to send graphic swooshes.","ok");ref.once("value").then(snap=>{if(!snap.exists())return ref.set(DEFAULT_STATE)});ref.on("value",snap=>{const data=snap.val();if(!data)return;state={...DEFAULT_STATE,...data,sold:{...(data.sold||{})}};hydrate();renderTeams()},err=>{console.error(err);setConn("Firebase listener error","bad");setCmd("Firebase listener error.","bad")})}else{setConn("Firebase not connected","bad");setCmd("Firebase not connected. Check js/firebase.js.","bad")};